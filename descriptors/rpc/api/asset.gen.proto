// Generated from APK FileDescriptorProto; original .proto source was not recovered.
syntax = "proto3";

package rpc.api;

import "google/api/annotations.proto";
import "google/protobuf/empty.proto";
import "options/check_option.proto";
import "options/zap.proto";
import "rpc/api/common/response.gen.proto";

option go_package = "github.com/qua-vision/vision-server/pkg/pb/rpc/api";
option csharp_namespace = "Vision.Common.Proto.Api";

message AssetListExpiredAssetIdResponse {
  .rpc.api.AssetListExpiredAssetIdResponse.HomeBanner home_banner = 1 [(options.zap.hidden) = true];
  .rpc.api.AssetListExpiredAssetIdResponse.ParkBanner park_banner = 2 [(options.zap.hidden) = true];
  .rpc.api.AssetListExpiredAssetIdResponse.Event event = 3 [(options.zap.hidden) = true];
  .rpc.api.AssetListExpiredAssetIdResponse.Marathon marathon = 4 [(options.zap.hidden) = true];
  .rpc.api.AssetListExpiredAssetIdResponse.LoginBonus login_bonus = 5 [(options.zap.hidden) = true];
  .rpc.api.AssetListExpiredAssetIdResponse.Shop shop = 6 [(options.zap.hidden) = true];
  .rpc.api.AssetListExpiredAssetIdResponse.StartupNotification startup_notification = 7 [(options.zap.hidden) = true];
  .rpc.api.AssetListExpiredAssetIdResponse.Gacha gacha = 8 [(options.zap.hidden) = true];
  .rpc.api.common.Response common_response = 9999;
  message HomeBanner {
    repeated string asset_ids = 1;
  }
  message ParkBanner {
    repeated string asset_ids = 1;
  }
  message Event {
    repeated string logo_asset_ids = 1;
    repeated string background_asset_ids = 2;
  }
  message Marathon {
    repeated string logo_asset_ids = 1;
    repeated string background_asset_ids = 2;
  }
  message LoginBonus {
    repeated string logo_asset_ids = 1;
    repeated string background_asset_ids = 2;
    repeated string thumbnail_asset_ids = 3;
  }
  message Shop {
    repeated string thumbnail_asset_ids = 1;
  }
  message StartupNotification {
    repeated string asset_ids = 1;
  }
  message Gacha {
    repeated string icon_asset_ids = 1;
    repeated string promotion_image_asset_ids = 2;
    repeated string promotion_movie_asset_ids = 3;
    repeated string bgm_asset_ids = 4;
    repeated string gacha_animation_asset_ids = 5;
  }
}

service Asset {
  rpc ListExpiredAssetId (.google.protobuf.Empty) returns (.rpc.api.AssetListExpiredAssetIdResponse) {
    option (options.check_option.check) = { enabled_response_cache: true };
    option (google.api.http) = { post: "/Asset/ListExpiredAssetId", body: "*" };
  }
}
